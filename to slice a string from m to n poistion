#include <stdio.h>
char* slice(char str[], int m, int n);
int main() {
    char str[]="Nilambar";
    printf("%s ", slice(str,3,5));

    return 0;
}
char* slice(char str[], int m, int n) {
    char *ptr1=&str[m];
    char *ptr2=&str[n];
    str=ptr1;
    str[n]='\0';
    return str;
}