// swap two numbers using pointers
#include <stdio.h>
void swap (int* a,int* b);

int main() {
    int x = 2;
    int y = 3;
    swap(&x,&y);
    printf(" x = %d and y = %d\n", x, y);

    return 0;
}

void swap (int *a , int *b) {
    int temp;
    temp = *a;
    *a = *b;
    *b = temp;
}