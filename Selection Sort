#include <stdio.h>
void selection_sort(int arr[],int n){
   for (int i=0; i<=n-2; i++){
       int min = i;
       for (int j = i+1; j<=n-1; j++){
           if (arr[j]<arr[min]){
               min = j;
           }
       }
       int temp = arr[min];
       arr[min]= arr[i];
       arr[i]= temp;
   }
}
int main() {
    int arr[8]={29,82,84,7,199,71,901,2};
    selection_sort(arr,8);
    for (int i = 0; i < 8; i++) {
    printf("%d ", arr[i]);
    }
    return 0;
}